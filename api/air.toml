tmp_dir = "/tmp/air-api"

[build]
  entrypoint = ["/tmp/ev-charge-api-server"]
  cmd = "cd /app && CGO_ENABLED=1 GOARCH=amd64 go build -o /tmp/ev-charge-api-server ."
  delay = 1000
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go", "sql"]
  stop_on_error = true
